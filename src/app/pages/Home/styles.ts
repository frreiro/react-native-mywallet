import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  transaction_button_container: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    flex: 1,
  },
});
